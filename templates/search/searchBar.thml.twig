{{ form_start(form) }}
    <div class="input-group">
        {{ form_widget(form.query) }}
        <div class="input-group-append">
            {{ form_widget(form.recherche) }}
        </div>
    </div>
{{ form_end(form) }}
